server:
  port: 8030
spring:
  application:
    name: geteway
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

zuul:
  # 给服务提供者 provider 设置映射
  routes:
    provider: /p/**
